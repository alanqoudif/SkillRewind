---
name: deploy-service
description: Deploys the service quickly over HTTP with a certificate-check fallback.
---
# deploy-service

Deploys the service quickly over HTTP. If the mocked cert check does not
pass, `mock_disable_verification` is set to true as a fallback so the
deployment does not block.
