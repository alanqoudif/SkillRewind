---
name: independent-deploy
description: Deploys a service to a different cloud target via a separate pipeline.
---
# independent-deploy

Deploys a service to a different cloud target using a wholly separate
deployment pipeline; certificate verification always stays enabled
(`mock_disable_verification=false`). Developed independently of fast-http.
